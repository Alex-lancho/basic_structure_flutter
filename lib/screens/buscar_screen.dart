import 'package:flutter/material.dart';

class BuscarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pantalla de Buscar', style: TextStyle(fontSize: 24)),
    );
  }
}
